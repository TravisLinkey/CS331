package cs.cs331;

/**
 * Created by travis on 4/8/17.
 */
public class UI {
    private programEngine engine = null;

    public UI(programEngine engine) {
        this.engine = engine;
//        engine.test.getInterSections(engine.test.getarray1(),engine.test.getarray2(),
//                engine.test.getBitSet1(),engine.test.getBitSet2(),
//                engine.test.getHashSet1(),engine.test.getHashSet2(),engine.getSize());
    }
}
